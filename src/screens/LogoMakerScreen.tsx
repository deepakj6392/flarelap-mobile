import React from 'react';
import LogoAndSticker from './editors/LogoAndSticker';

export default function LogoMakerScreen({ route, navigation }: any) {
  return <LogoAndSticker route={route} navigation={navigation} />;
}

